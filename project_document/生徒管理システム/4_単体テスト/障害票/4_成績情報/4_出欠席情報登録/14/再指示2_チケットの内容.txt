問題
  備考のByte数チェックを発生させ、背景色を変えた後
  備考のByte数を制限値(小は60byte、中は104byte)まで戻して登録ボタンを押しても
  背景色の色が元に戻らない。

対応内容
  備考が制限値ちょうどのByte数の場合は
  背景色を通常の色にするようにしてください。

  また、現在、中学校の備考は104Byteを制限値としていますが
  通知票のレイアウトが少し変わるため、制限値を100Byteに変更してください。