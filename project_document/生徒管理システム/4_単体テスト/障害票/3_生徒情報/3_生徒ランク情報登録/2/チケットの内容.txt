問題
  初期表示時にGradeとClassとReport Subjに値が設定されている。

対応内容
  Grade、Class、Report Subjのセレクトボックスは、上位のセレクトボックスが選択されるまで、disabledにしてください。
  もし、実装が難しそうであれば、Pupils Info Searchで行っているセレクトボックスの連動処理のように
  それぞれのセレクトボックスが選択されるたびに、テーブルより内容を取得するように実装してかまいません。